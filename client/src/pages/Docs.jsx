import React from "react";

// const getDocsMetadata = () => {
//   const folder = "markdown/";
//   const files = fs.readdirSync(folder);
//   const markdownDocs = files.filter((file) => file.endsWith(".md"));
//   const slugs = markdownDocs.map((file) => file.replace(".md", ""));
//   return slugs;
// };

export default function Docs() {
  // const docsMetaData = getDocsMetadata();
  // const docsPreview = docsMetaData.map((slug) => (
  //   <div>
  //     <h2>{slug}</h2>
  //   </div>
  // ));

  return <div>yoo</div>;
}
